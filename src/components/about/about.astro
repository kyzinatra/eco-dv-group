---
import AboutCard from "./about-card/about-card.astro";
import { aboutCards } from "./about.constants";
import css from "./about.module.css";
---

<section class={css.about} id="about">
	{
		aboutCards.map((el) =>
			el.content ? (
				<AboutCard src={el.src} className={el.className} alt="">
					{el.content}
				</AboutCard>
			) : (
				<AboutCard src={el.src} className={el.className} alt="" />
			)
		)
	}
</section>
