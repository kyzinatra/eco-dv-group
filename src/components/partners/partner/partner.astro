---
import { Image } from "astro:assets";
import css from "./partner.module.css";
interface Props {
	src: ImageMetadata;
	alt: string;
}

const { src, alt } = Astro.props;
---

<Image src={src} alt={alt} class={css.partner} />
